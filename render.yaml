services:
- type: pserv
  name: odiopay-live-be-db
  plan: standard
  env: docker
  autoDeploy: false
  disk:
    name: odiopay-live-be-db-disk
    mountPath: /var/lib/mysql
    sizeGB: 10
  envVars:
  - fromGroup: odiopay-live-be-db

