services:
- type: pserv
  name: miles-soap-live-db
  plan: standard
  env: docker
  autoDeploy: false
  disk:
    name: miles-soap-live-db-disk
    mountPath: /var/lib/mysql
    sizeGB: 10
  envVars:
  - fromGroup: miles-soap-live-db-env

envVarGroups:
- name: miles-soap-live-db-env
  envVars:
  - key: MYSQL_DATABASE
    value: miles-soap-live-db
  - key: MYSQL_USER
    value: miles-soap-live-db-user
  - key: MYSQL_PASSWORD
    generateValue: true
  - key: MYSQL_ROOT_PASSWORD
    generateValue: true

